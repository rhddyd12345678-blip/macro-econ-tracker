# 작업 보고서 — '시장 전망' 탭 구현

작업 시각: 2026-09-17. `시장전망_출처조사.md`(이전 턴 조사)를 그대로 구현했다.

## 1. 출처별 수집 결과 (실제 실행 로그 기준, `--full` 강제 실행)

| 출처 | 상태 | 비고 |
|---|---|---|
| IMF WEO DataMapper | ✅ 성공 | 30건(5개국 × 3지표 × 2개년) |
| OECD Economic Outlook | ✅ 성공 | 30건(dataflow v1.5 = Economic Outlook 119, 버전 자동 탐지 확인) |
| 연준 SEP | ✅ 성공 | 8건(2026-09-16 발표분, GDP·실업률·PCE물가·정책금리 중간값 × 2개년) |
| ECB staff projections | ✅ 성공 | 4건(유로존 집계, **국가별 분해 없음** — 조사 때 남긴 한계가 실제로 확인됨) |
| 필라델피아 연준 SPF | ✅ 성공 | 4건(CPI·실업률 × 2개년) |
| ECB SPF | 🚫 차단 | `data-api.ecb.europa.eu`가 "Your access has been blocked due to security concerns"로 응답(WAF) — User-Agent 변경 등 우회 시도 없이 차단으로 기록하고 종료. `forecasts_manual.csv`에 수동 입력 대상으로 남김 |
| ECOS 시장금리(통안증권 1년·국고채 3년) | ✅ 성공 | 50건(월평균, 최근 2년치) |
| FRED 시장금리(2년물·5년·10년 기대인플레이션) | ✅ 성공 | 75건(월평균, 최근 2년치) |

**총 누적**: `data/forecasts.csv` 201행(헤더 제외). 이번 환경에서 IMF·ECOS·FRED 등 여러 호스트에 순차적으로 요청을 보낼 때 간헐적 DNS/연결 실패가 있었으나(이 샌드박스의 일시적 네트워크 불안정으로 판단 — 같은 호스트를 잠시 후 재시도하면 성공), 재시도 후에는 모든 소스가 정상적으로 데이터를 받아왔다. 재현되면 안 되는 문제이므로 GitHub Actions(완전히 다른 네트워크 환경)에서의 실제 실행 결과로 최종 확인했다(§4).

## 2. 수동 입력이 필요한 항목과 다음 발표 예정일

`data/forecasts_manual.csv`는 헤더만 있고 값은 비어 있다(예시 행 없음, 요청대로). 아래 항목을 사람이 채워야 한다:

| 기관 | 최근 발표(조사 시점 기준) | 다음 발표 예정(대략) | 비고 |
|---|---|---|---|
| 한국은행 경제전망보고서 | 2026년 8월 | 2026년 11월경 | 연 4회(2·5·8·11월) |
| 일본은행 Outlook Report | 2026년 7월 31일 | 2026년 10월경 | 연 4회(1·4·7·10월), PDF만 |
| 금투협 BMSI 기준금리 방향 | 2026년 9월(응답: 동결 79%·인상 20%) | 2026년 10월 초 | 매월 발표 |
| JCER ESP Forecast | 매월(최신월 확인 필요) | 익월 초 | 일본 GDP 컨센서스 |
| ECB SPF | 이번 자동 수집에서 차단됨 | 2026년 10월 30일(잠정, ECB 페이지 안내) | 자동 수집 재시도가 계속 실패하면 이 항목만 수동 입력 |

`docs/수동전망입력.md`에 각 출처별로 숫자를 찾는 정확한 위치(URL)와 CSV 작성법을 정리했다.

## 3. 설계 판단

1. **forecasts.csv 스키마를 시장가격에도 그대로 적용**: 통안증권·국고채·FRED 시리즈는 "전망"이 아니지만, 요청하신 8열 스키마(발표일|기관|종류|국가|지표|대상기간|전망치|출처URL)를 그대로 써서 종류="시장가격"으로 구분했다. 대상기간은 "YYYY-MM"(그 달의 평균), 발표일은 그 달의 1일로 통일해 다른 행들과 정렬 가능하게 했다. 원래 논의됐던 "최신값"은 시장 전망 탭에서 시리즈의 마지막 원소를 그대로 읽어 쓰는 방식으로 처리하고, 별도로 저장하지는 않았다(하루마다 값이 바뀌는 것을 굳이 vintage로 누적할 이유가 없다고 판단).
2. **기관 전망 결과와 시장가격 결과를 한 파일에 같이 쌓되, 기관 전망만 월요일 제한**: `is_monday` 플래그로 감싸 요청량을 줄였고, 수동 테스트를 위해 `--full` 플래그로 요일 제한을 무시할 수 있게 했다(운영에는 영향 없음).
3. **차단 판정 로직 버그를 검증 중 직접 발견해 고쳤다**: 처음엔 응답 본문 앞 2000자만 검사해서 ECB Data Portal의 차단 문구("your access has been blocked...")를 놓쳤다(차단 페이지가 base64 인라인 이미지로 앞부분 11KB를 채우고 있었음). 전체 본문(최대 20만자)으로 검사 범위를 넓혀 실제로 "차단"으로 정확히 잡히는 것을 재실행으로 확인했다.
4. **OECD 물가지표는 이번 자동 수집에서 뺐다**: `CPI` 측정코드가 실제로는 YoY %가 아니라 지수 레벨로 보여(예: 1.19 같은 값), 직접 증감률로 환산하는 로직을 추가하면 검증 없이 틀린 값을 내보낼 위험이 있다고 판단해, 물가는 IMF(PCPIPCH, 이미 YoY %로 직접 제공)로만 커버하기로 했다. GDP·실업률·단기금리(정책금리 근사)는 OECD 코드가 명확해 그대로 썼다.
5. **필라델피아 연준 SPF는 CPI·실업률만, RGDP는 뺐다**: SPF의 연간 컨센서스 파일에서 실질GDP 성장률에 대응하는 "당해/내년" 열(A/B 접미사)을 이번 조사에서 찾지 못했다(공식 문서 PDF가 파싱되지 않아 열 정의를 확증하지 못함). CPIA/CPIB, UNEMPA/UNEMPB는 Philadelphia Fed의 잘 알려진 표기 관례(당해년/내년 연평균)로 비교적 확신을 갖고 매핑했지만, 확실하지 않은 열을 억지로 GDP에 끼워 맞추기보다는 빼는 쪽을 택했다.
6. **ECB projections는 유로존 집계만, 독일·프랑스 개별 분해 없음**: 실제로 다운로드해 시트를 열어본 결과 국가별 분해 열이 없었다(조사 때의 우려가 사실로 확인됨). 그래서 국가별 전망 탭에 "유로존"을 독일·프랑스와는 별개의 6번째 탭으로 뒀고, 독일·프랑스 탭에는 IMF·OECD 값만 나온다.
7. **미국·유로존 기대인플레이션 → 실제로는 미국만**: 요청하신 "미국·유로존 기대인플레이션(T5YIE·T10YIE)"에서 T5YIE·T10YIE는 둘 다 FRED가 제공하는 미국 BEI(Breakeven Inflation) 시리즈이고, 유로존에 대응하는 자동 수집 가능한 기대인플레이션 시리즈는 이번 조사에서 찾지 못했다. 차트 제목을 "미국 5년·10년 기대인플레이션(BEI)"로 정확히 표기하고, 없는 유로존 시리즈를 지어내지 않았다.
8. **금통위 전망 탭 요약 2줄**: `collectBokFacts()`에 "BMSI 최신 응답 비율"과 "통안증권(1년) - 기준금리"를 추가했다. 이 함수는 라이브 패널과 저장 스냅샷이 공유하므로, 별도 작업 없이 저장 시에도 두 줄이 함께 기록된다(기존 코드 구조를 그대로 활용).
9. **스프레드 계산에 기존 analysis.json의 정책금리 월별 시계열을 재사용**: `rate_linkage.py`가 이미 만들어 둔 `market.policy... series.KR`(월말 기준 계단형 기준금리)을 그대로 읽어, 별도로 기준금리 스텝 함수를 다시 만들지 않았다. forecasts.json의 시장금리(월평균, YYYY-MM)와 월 키로 맞춰 스프레드를 계산한다.

## 4. 검증

- **forecasts.json 필드 전수 대조**: 시장 전망 탭·금통위 전망 패널 JS 코드에서 참조하는 모든 필드(발표일/기관/종류/국가/지표/대상기간/전망치/응답비율_동결/인상/인하/출처URL/비고, collection_results의 출처/상태/비고)가 실제 forecasts.json에 정확히 그 이름으로 존재하는지 grep으로 대조 확인했다.
- **수동 CSV 빈 상태 확인**: `data/forecasts_manual.csv`가 헤더만 있는 상태에서 `forecast_collect.py`를 실행해 `load_manual_rows()`가 빈 리스트를 반환하고(발표일·전망치 없는 행은 건너뜀), 탭 쪽은 BMSI "수동 입력 대기", 한국은행 전망 "수동 입력 대기"로 정상 표시되도록 가드 처리했다.
- **HTML/JS 정합성**: 브레이스·괄호 균형, 태그 짝, 중복 id, 함수명 중복 검사 모두 통과. diff로 기존 세 탭(국가별 지표·금리 연동 분석·금통위 전망) 코드는 전혀 건드리지 않았고, `collectBokFacts()`에 두 줄을 추가한 것과 데이터 로딩부에 `forecasts.json` 한 줄을 추가한 것 외에는 전부 신규 추가임을 확인했다.
- **로컬 서버**: `python3 -m http.server`로 `data/forecasts.json` 등 전부 200 확인.
- **실제 값 대조**: 연준 SEP(GDP 2.3/2.4%, 실업률 4.1/4.1%, PCE물가 3.7/2.3%, 정책금리 중간값 4.1/4.1%), ECB projections(GDP 0.9/1.4%, HICP 3.0/2.5%), IMF 한국(GDP 1.9/2.1%, 물가 2.5/1.9%, 실업률 2.8/2.9%) 등을 이전 턴의 조사 결과 및 원문 페이지에서 직접 읽은 수치와 대조해 전부 일치 확인.
- **GitHub Actions 수동 실행**(`workflow_dispatch`, 실행 ID `35203851610`) — **성공**. `collect.py → analysis/rate_linkage.py → export.py → forecast_collect.py` 체인이 GitHub의 네트워크 환경(이 작업을 하던 로컬 샌드박스와는 별개)에서 끝까지 오류 없이 실행됨을 확인했다. 로그 기준: 기관 전망은 실행일(수요일)이 월요일이 아니라 의도대로 건너뛰었고, ECOS 시장금리 50건·FRED 시장금리 75건 모두 성공(이미 당일 수집한 값이라 신규 0건, 기존 201건 유지 — 중복 저장 방지 로직도 함께 확인됨). 워크플로가 변경된 `site/data/*`를 자동 커밋·푸시했고(`186f257`), 이어서 Pages 배포(`workflow_run` 트리거, 실행 ID `35203960017`)도 성공해 실제 서비스 페이지에서 "시장 전망" 탭과 `data/forecasts.json`(HTTP 200) 응답을 확인했다.

## 5. 만든 파일 / 수정한 파일

- `forecast_collect.py` (신규) — 자동 수집 스크립트
- `data/forecasts.csv` (신규, 커밋 대상) — 누적 vintage 데이터
- `data/forecasts_manual.csv` (신규, 헤더만) — 수동 입력용
- `docs/수동전망입력.md` (신규) — 수동 입력 가이드
- `collect.py` (수정) — 체인 끝에 `forecast_collect.py` 호출 추가
- `requirements.txt` (수정) — `lxml` 추가(연준 SEP HTML 표 파싱용 `pandas.read_html`에 필요)
- `site/index.html` (수정) — "시장 전망" 탭 추가(상단 한국 금통위 컨센서스, 하단 국가별 전망, 수집 결과 표), 금통위 전망 판단 근거 패널에 BMSI·스프레드 2줄 추가
- `site/data/forecasts.json` (자동 생성물)

## 6. 실패하거나 보류한 항목

- **ECB SPF 자동 수집**: WAF 차단으로 실패, 수동 입력 대상으로 전환(설계상 의도된 동작).
- **OECD 물가지표**: 지수 레벨과 증감률을 혼동할 위험 때문에 이번 범위에서 제외(§3-4). 필요하면 다음 작업에서 YoY 환산 로직을 검증 후 추가할 수 있다.
- **필라델피아 연준 SPF의 실질GDP 성장률**: 열 정의를 확증하지 못해 제외(§3-5).
- **ECB projections의 독일·프랑스 개별 전망**: 원본 데이터 자체에 없음(구조적 한계, 코드 문제 아님).
- **유로존 기대인플레이션 시리즈**: 자동 수집 가능한 출처를 찾지 못해 미국 시리즈만 제공.

## 7. 추가 작업 — 수동 데이터 반영, 연준 금리 인상, ECB 발표일 확인 (후속 요청)

### 7-1. data/forecasts_manual.csv에 25건 반영

사용자가 공식 발표·보도에서 직접 확인한 값을 그대로 입력했다(수치는 수정하지 않음). 반영 과정에서 실제 데이터로 처음 채워보니 두 가지가 설계 당시 가정과 달랐다:

1. **기관·지표 명칭이 처음 설계한 placeholder와 다름**: "금융투자협회"→"금융투자협회 BMSI", "정책금리방향"→"기준금리 방향", "JCER"→"JCER ESP", "ECB"→"ECB SPF" 등. 사용자가 준 실제 명칭을 그대로 CSV에 넣고, 대신 `forecast_collect.py`의 `MANUAL_INSTITUTION_KIND`와 `site/index.html`의 모든 조회 코드(BMSI 막대, 한국은행 전망 박스, 금통위 판단 근거 패널)를 실제 명칭에 맞게 고쳤다(데이터를 코드에 맞추지 않고 코드를 실제 데이터에 맞춤). `docs/수동전망입력.md`도 실제 명칭으로 갱신.
2. **검증 중 직접 발견한 버그**: `load_manual_rows()`가 "발표일과 전망치가 둘 다 있어야" 행을 읽어들이도록 돼 있어서, 전망치 없이 응답비율만 있는 설문형 행(BMSI 기준금리 방향, JCER 정책금리 방향)이 25건 중 2건 조용히 빠졌다(23건만 로드됨). "전망치 또는 응답비율 중 하나라도 있으면" 포함하도록 고치고 재실행해 25건 전부 반영되는 것을 확인했다.

### 7-2. BMSI 표시 개선(요청 1)

`extractMeetingDate()`/`extractActualResult()`를 추가해 대상기간 문자열("2026-08-27 금통위")에서 날짜를 뽑고, 오늘 날짜와 비교해 지났으면 "지난 조사 (대상 금통위 2026-08-27) · 실제 결과: 인상"으로, 아직이면 "대상 금통위: OOOO-OO-OO (예정)"으로 표시하도록 시장 전망 탭과 금통위 전망 판단 근거 패널 양쪽에 적용했다. 실제 데이터로 Python에 포팅해 재현한 결과 정확히 "지난 조사 (대상 금통위 2026-08-27) · 실제 결과: 인상"이 나오는 것을 확인했다(오늘 2026-09-17 기준).

### 7-3. 연준 정책금리 인상(요청 2) — 확인 결과

- **로우데이터**: 반영 안 돼 있었음(마지막 행 2025-12-10=3.75%).
- **FRED DFEDTARU 직접 조회**: 2026-09-16까지도 여전히 3.75%로, **FRED 자체가 아직 새 값을 발행하지 않은 상태**였다(적용일 관행상 보통 결정 다음 영업일에 새 값이 나타나므로, 이번 세션 시점에서는 아직 게시 전).
- **연준 공식 보도자료로 교차 확인**: `federalreserve.gov/newsevents/pressreleases/monetary20260916a.htm`에서 "raise the target range for the federal funds rate by 1/4 percentage point to 3-3/4 to 4 percent"(12-0 만장일치)를 직접 확인했다. FRED가 아직 게시하지 않았지만 1차 출처(연준 공식 성명)로 사실 자체는 확정적이라고 판단해, 이 프로젝트의 기존 관례(기준일=발표일=결정일, FRED의 '적용일'이 아님)에 따라 **2026-09-16 / 4.00%** 행을 직접 추가했다. 추가 전 백업, 추가 후 zip-diff로 로우데이터 시트의 딱 한 행(1810행)만 바뀌었고 다른 셀·시트는 전혀 건드리지 않았음을 확인했다.
- **한미 정책금리차**: 최신 관측치(`data.json`)에는 즉시 4.00%로 반영되어 "시장 전망" 탭의 "현재 정책금리" 표시는 바로 -1.00%p 스프레드를 계산할 수 있는 상태가 됐다. 다만 `analysis.json`의 월별 스프레드 시계열(`rate_linkage.py`가 "가장 최근 완료된 달" 기준으로 만듦, 이전 턴에서 확정한 설계)은 **9월이 아직 끝나지 않아 8월(-0.75%p)까지만** 나온다 — 이건 버그가 아니라 기존 설계가 의도한 동작이며, 9월이 끝나고 나면(10월 첫 실행부터) 자동으로 -1.00%p가 반영된다.

### 7-4. ECB staff projections 발표일 확인(요청 3)

ECB 발표 페이지(`ecb.europa.eu/press/projections/html/index.en.html`)의 "About the projections" 바로 뒤에 나오는 "10 September 2026 ECB staff macroeconomic projections for the euro area, September 2026" 문구로 **2026년 9월 10일 발표(ECB staff, 9월판)**임을 직접 확인했다. 자동 수집 로직은 파일명(YYYYMM)만으로 "그 달 1일"로 근사해 저장했었는데(2026-09-01, 실제와 9일 차이), `_ecb_precise_vintage_date()`를 추가해 페이지 텍스트에서 정확한 일자를 파싱하도록 고치고, 이미 저장된 4개 행의 발표일도 2026-09-01→2026-09-10으로 고쳤다(같은 vintage의 날짜 오차 수정이지 새 vintage 추가가 아니므로 행을 늘리지 않고 직접 수정). 3월·6월·9월·12월이 번갈아 "ECB staff"/"Eurosystem staff" 명칭을 쓰는 것도 이번에 확인했다(3월·9월=ECB staff, 6월·12월=Eurosystem staff).

### 7-5. 검증

- 실제 forecasts.json에서 BMSI·JCER 설문형 행이 정상적으로 응답비율만 가진 채 포함됨을 확인.
- BMSI 표시 로직을 Python으로 포팅해 오늘 날짜 기준 정확한 문구가 나오는 것을 확인.
- xlsx 추가 행을 zip-diff로 대조해 의도한 1행 외 어떤 셀도 바뀌지 않았음을 확인.
- 미국 정책금리 로우데이터 최신 행(2026-09-16, 4.00%)과 analysis.json의 월별 스프레드(8월까지만, 의도된 지연)를 함께 확인.
- ECB 발표일 수정 후 forecasts.csv/json 양쪽에서 "2026-09-10"으로 일치함을 확인.
- HTML 태그 짝·중복 id·함수명 중복 검사, 로컬 서버 200 확인.
